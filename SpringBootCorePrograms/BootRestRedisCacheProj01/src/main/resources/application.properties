#Application name
spring.application.name=BootRestRedisCacheProj01

#MySQL JDBC Properties 
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/jpadb
spring.datasource.username=root
spring.datasource.password=ILoveCoding@8901

#JPA/Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update   
# Auto-creates/updates tables (CACHE_PRODUCT will be created)

#Embedded Tomcat Server port
# server.port=4041

#Caching Configuration(Redis as cache provider)
spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379


# Optional: Redis connection pooling (for production)
# spring.data.redisLettuce.pool.max-active=8
# spring.data.redisLettuce.pool.max-idle=8
# spring.data.redisLettuce.pool.min-idle=0